{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://paperkg.local/schema/predicate-registry-0.2.0.json",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "const": "0.2.0" }
  },
  "additionalProperties": {
    "type": "array",
    "items": {
      "enum": ["part_of_version", "addresses", "partially_addresses", "leaves_open", "reframes", "inherits_problem", "introduces_problem", "reopens", "uses_method", "extends_method", "modifies_method", "combines_with", "replaces_component", "removes_component", "supports", "contradicts", "qualifies", "critiques", "provides_evidence_for", "lacks_evidence_for", "extends", "builds_on", "compares_against", "reproduces", "fails_to_reproduce", "supersedes", "is_version_of", "uses_benchmark", "evaluates_on", "reports_metric", "uses_split", "uses_protocol", "reports_result", "chronologically_after"]
    }
  }
}
